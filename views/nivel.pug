//-         // Config SIN LAYOUT
//- html
//-     head
//-         title="mi primera pagina Pug JS"
//-     body
//-         // - block medidor
//-         // el guion permite definir variables
//-         - const estilo = "width:25%; height:20px"
//-         // el simbolo != permite insertar html parseado en lugar del string
//-         h1(style="color:blue")!=titulo 
//-         span=min+" "
//-         meter(value=nivel min=min max=max style=estilo)
//-         span=" "+max
//-         //querystring http://localhost:7001/datos?min=0&max=200&nivel=75&titulo=%3Ci%3EMedidor%3C/i%3E
//-         // Config SIN LAYOUT

        //- // Config CON LAYOUT
extends layout 

block medidor 
        // el guion permite definir variables
        - const estilo = "width:25%; height:20px"
        // el simbolo != permite insertar html parseado en lugar del string
        h1(style="color:blue")!=titulo 
        span=min+" "
        meter(value=nivel min=min max=max style=estilo)
        span=" "+max
        //querystring http://localhost:7001/datos?min=0&max=200&nivel=75&titulo=%3Ci%3EMedidor%3C/i%3E
        //- // Config CON LAYOUT
